{
    "version": 2,
    "builds": [
        {
            "src": "*.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/candidates(.*)",
            "dest": "/api/candidates$1"
        }
    ]
}